# SkyRoads 重设计执行计划

## 背景诊断（原 index.html 的问题）

1. **画面/碰撞脱节**：投影使玩家当前 ~20 段跑道渲染在屏幕外，飞船却画在对应前方 ~36 段的位置 → 死亡原因在屏幕上不可见，体验"死得莫名其妙"。
2. **跳跃无法越过墙体**：墙高 700 < 跳跃顶点 ~1687，但碰撞逻辑不看高度，撞墙必死。
3. **赛道可解性无保障**：可能出现必须"变道+起跳同时完成且落点无安全车道"的死局。
4. **手感**：初速 0、13 秒才满速；碰撞按单点采样，高速下有穿段风险。

## 阶段 1 — 重写游戏（coder 子代理：重构工程师_01）

- 完全重写 `/Users/stan/Developer/Local/SkyRoads/index.html`，保持单文件、零依赖、Canvas 2D、中文 UI、localStorage 最佳记录、原操作方式。
- 硬性要求：
  - 玩家飞船与跑道共用同一套投影，画面所见 = 碰撞所判；玩家当前段必须可见。
  - 跳跃可越过墙（高度判定与视觉一致）；碰撞做扫掠检测防穿段。
  - 赛道生成保证可解：每段至少一条安全车道；相邻段安全车道可达（考虑变道耗时与当前速度）；跳跃落点必有安全车道。
  - 手感：合理初速与加速曲线。
- 同步更新 README.md（操作/机制如有变化）。
- 输出：新 index.html + 更新后的 README.md。

## 阶段 2 — 验收（Orchestrator 本人）

- `node --check` 校验脚本语法（提取 script 标签内容）。
- 结构审查：投影一致性、可解性生成逻辑、状态机完整性。
- 不合格 → 带着具体问题回炉阶段 1。

## 阶段 3 — 交付

- 向用户汇报问题清单、修复方案、文件路径。
