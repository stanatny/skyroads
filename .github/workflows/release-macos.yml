name: Build macOS release

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  build-and-upload:
    runs-on: macos-latest
    timeout-minutes: 15

    steps:
      - name: Check out release tag
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Validate release version contract
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: node scripts/check-release-tag.js "$RELEASE_TAG"

      - name: Validate web game
        run: |
          npm test
          npm run check

      - name: Build and verify universal app
        run: |
          bash tests/app-universal-smoke.sh
          bash tests/app-signature-smoke.sh
          bash tests/app-resources-smoke.sh
          bash tests/app-wkwebview-smoke.sh
          plutil -lint "Nebula Cruise.app/Contents/Info.plist"

      - name: Package app
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          archive="Nebula-Cruise-macOS-${RELEASE_TAG}.zip"
          ditto -c -k --sequesterRsrc --keepParent "Nebula Cruise.app" "$archive"
          shasum -a 256 "$archive"

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          gh release upload "$RELEASE_TAG" "Nebula-Cruise-macOS-${RELEASE_TAG}.zip" --clobber
