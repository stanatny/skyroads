{
  "name": "nebula-cruise",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "for file in src/*.js; do node --check \"$file\"; done"
  }
}
